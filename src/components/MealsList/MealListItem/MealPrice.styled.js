import styled from "styled-components";

const MealPriceContainer = styled.div`
  margin-top: 0.5rem;
  font-weight: bold;
  color: #ad5502;
  font-size: 1.25rem;
`;

export default MealPriceContainer;
